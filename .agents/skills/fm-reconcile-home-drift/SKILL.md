---
name: fm-reconcile-home-drift
description: Reconcile home drift.
user-invocable: true
---

# fm-reconcile-home-drift

Start from fresh tools plus AGENTS.md section 1 truth order and layer contract.
Each item gets one owner layer and one verb: keep, merge, relocate, compile, quarantine, or drop.
No fact may live in tracked template and local fleet layers.
Quarantine is fail-closed: no delete or overwrite until live, remote, or runtime evidence verifies the final verb.
After cap-approved remote rewrite elsewhere, only `sbin/fm update --adopt-remote`; trust its clean-tree/diverged-history/no-local-only gates, never hand-reset.
Done means every item has owner, verb, relevant home, and no duplicate fact.
